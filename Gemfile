source "https://rubygems.org"

gem "jekyll", "~> 4.4"
# gem "webrick"        # required when using Ruby >= 3 and Jekyll <= 4.2.2

gem "just-the-docs", "0.12.0" # pinned to the current release; bump this line to upgrade the theme
gem "jekyll-sitemap"
# Rewrites relative *.md links to their built URLs, so cross-links work both on
# GitHub (raw Markdown) and on the published site.
gem "jekyll-relative-links"
